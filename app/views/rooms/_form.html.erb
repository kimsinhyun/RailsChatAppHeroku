<%= form_with(model: @room, remote: true, class: 'd-flex') do |f| %>
  <%= f.text_field :name, class:' form-control', autocomplete: false %>
  <%= f.submit 'Create', data: { disable_with: false },class: 'btn btn-primary ml-2' %>
<% end %>